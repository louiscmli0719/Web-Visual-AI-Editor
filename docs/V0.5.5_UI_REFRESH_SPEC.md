# Web Visual AI Editor V0.5.5 UI Refresh Spec

## 1. Problem / Opportunity

V0.1-V0.5 的功能闭环已经完整（选元素 -> 评论 -> 样式 -> 测距 -> 共享分组 -> 导出），但 UI 需要从早期原型风格收敛为更像 Sketch 右侧 Inspector 的专业工具质感：哑光深黑面板、紧凑区块、深灰胶囊控件、弱边界、低发光。

V0.5.5 的机会：在不破坏现有功能的前提下，重构 UI 表层，让插件面板像设计工具属性面板，而不是悬浮装饰面板。

## 2. Goal and Success Metrics

目标：将 Panel 视觉升级为 Sketch-style matte inspector，Overlay 保持红粉 / 红色高对比反馈，所有 V0.5 功能完整可用。

当前实现状态（2026-05-24）：代码整合已完成并通过 `npm run verify`（89 个测试 + TypeScript + Vite build），等待 Chrome 手动视觉与交互复核。实现中复用 `overlay-root.ts` 已有 Shadow DOM 尺寸标签承载元素标签，避免新增宿主页面样式注入点；Panel 标题栏支持自由拖动，并在视口内约束位置，数值输入改为“左数值 / 右单位”的胶囊控件，记录会按实际切换后的单位保存。

成功标准：

1. 哑光深色主题：Panel 主背景 `rgba(18, 18, 18, 0.97)`，文字高对比。
2. Sketch Inspector 质感：深灰胶囊控件、弱边界、少阴影、无大面积 blur。
3. 浮动工具栏顶部固定，选择、测距、记录、关闭等核心操作可用。
4. 蓝紫强调色只用于按钮、徽标、Panel 焦点状态。
5. Overlay 选中框、测距线和相似元素高亮使用红粉 / 红色高对比反馈。
6. 记录列表使用紧凑 inspector 行块，不使用独立发光卡片。
7. 所有 V0.5 功能（评论、样式、测距、共享分组）在新 UI 下正常工作。
8. Panel 宽度默认约 360px，内容在桌面浏览器内可滚动。
9. Panel 默认仍贴右侧显示，用户可拖动标题栏自由移动，拖动后不会超出视口。
10. 数值输入采用左侧数值、右侧单位的胶囊样式，默认 `px`，可切换 `pt`，清空后自动归零。

## 3. Target Users and Main Scenarios

| 用户 | 场景 | V0.5.5 要解决的问题 |
|---|---|---|
| AI 编程用户 | 长时间使用工具标注页面 | 深色工具面板减少眼疲劳，控件层级清晰 |
| 产品经理 | 演示标注结果给团队 | 专业设计工具质感提升可信度 |
| UI/UX 设计师 | 对比设计稿和实现 | Sketch-like Inspector 更符合设计工作流习惯 |
| 前端开发者 | 快速定位元素 | 浮动工具栏 + 元素标签提升操作效率 |

## 4. Design System

### 4.1 Color Palette

**Sketch-style Matte Base:**

- Background primary: `#121212`
- Background secondary: `#181818`
- Background tertiary: `#202020`
- Panel background: `rgba(18, 18, 18, 0.97)`
- Toolbar background: `rgba(20, 20, 20, 0.98)`
- Section surface: `rgba(255, 255, 255, 0.055)`
- Field surface: `rgba(255, 255, 255, 0.085)`
- Field hover: `rgba(255, 255, 255, 0.11)`
- Border: `rgba(255, 255, 255, 0.08)`
- Strong border: `rgba(255, 255, 255, 0.18)`

**Text:**

- Primary: `rgba(255, 255, 255, 0.95)`
- Secondary: `rgba(226, 232, 240, 0.82)`
- Tertiary: `rgba(148, 163, 184, 0.78)`

**Accent (Inspector Blue-Violet):**

- Primary fill: `#5b6cff`
- Hover fill: `#6b7aff`
- Accent secondary: `#9aa4ff`
- Soft fill: `rgba(91, 108, 255, 0.16)`

**Overlay:**

- Selected element: `rgba(255, 45, 85, 0.14)` border `#ff2d55` 2px solid
- Measurement: `rgba(239, 68, 68, 0.1)` line / border `#ef4444`
- Similar elements: `rgba(251, 113, 133, 0.14)` border `#fb7185` 1px dashed

### 4.2 Typography

```css
--font-sans: -apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans SC", sans-serif;
--font-mono: "SF Mono", ui-monospace, Menlo, Consolas, monospace;
```

- Panel title: 15px / 20px / 600
- Section title: 14px / 20px / 700
- Body: 13px / 18px / 400
- Caption: 11-12px / 16-18px / 400
- Numeric controls: 15px / 18px / 600 (mono, tabular nums)

### 4.3 Inspector Surfaces

**Panel:**

```css
background: rgba(18, 18, 18, 0.97);
border: 1px solid rgba(255, 255, 255, 0.1);
border-radius: 22px;
box-shadow: 0 24px 70px rgba(0, 0, 0, 0.38), inset 0 1px 0 rgba(255, 255, 255, 0.05);
```

**Inspector Section:**

```css
margin: 22px 22px 0;
padding: 0;
background: transparent;
border: 0;
box-shadow: none;
```

**Control / Field:**

```css
min-height: 40px;
border-radius: 12px;
background: rgba(255, 255, 255, 0.085);
border: 1px solid transparent;
```

**Length Capsule:**

```css
display: flex;
align-items: stretch;
min-height: 40px;
border-radius: 14px;
overflow: hidden;
```

**Focus:**

```css
border-color: rgba(91, 108, 255, 0.78);
box-shadow: inset 0 0 0 1px rgba(91, 108, 255, 0.24);
```

### 4.4 Interaction

- Hover: subtle field background lift only.
- Focus: blue-violet inset ring.
- No scale animation for inspector rows.
- No glow as primary affordance.
- Respect `prefers-reduced-motion`.

## 5. Component Architecture

### 5.1 Floating Toolbar

- Top fixed toolbar outside Panel scroll area.
- Matte dark background.
- Active state uses soft blue-violet fill + inset border.
- Hover state uses subtle field lift.

### 5.1.1 Draggable Panel

- Panel 默认保持右侧 Inspector 位置，减少首次进入编辑模式时的认知成本。
- 用户可拖动 Panel 标题栏移动面板，避开正在检查的页面区域。
- 拖动位置只在当前编辑会话内生效，不写入 JSON、不写入本地存储。
- 拖动过程使用 Pointer Events，并约束在视口 8px 安全边距内，窗口尺寸变化后自动夹回可见区域。

### 5.2 InspectorSection

`InspectorSection` replaces the previous heavy section wrapper. It is a semantic layout wrapper only; visual hierarchy comes from spacing, titles, dividers and compact controls rather than heavy cards.

### 5.3 Element Info Panel

Inspector section with compact tag, selector and numeric control cells:

```text
p                    962 x 23 px
----------------------------
位置与尺寸
[ X 445 ] [ Y 408 ]
[ W 962 ] [ H 23  ]
> 高级信息
```

### 5.4 Style Editor

Sketch-like rows with labels on the left and rounded dark controls on the right。长度输入使用左数值、右单位的胶囊控件，默认 `px`，支持 `pt` 切换，空输入自动归零：

```text
样式编辑
> 外观
文本颜色      [ rgb(148, 163, 184) ]
背景色        [ rgba(0, 0, 0, 0)   ]
圆角          [ 12 | px ▾          ]
> 排版
字号          [ 16 | px ▾          ]
```

### 5.4.1 Length Unit Capsule

- 数值输入左侧只接受数值，右侧单位下拉默认 `px`。
- 从 `px` 切到 `pt` 时自动换算，不要求用户手算。
- 清空数值后自动回到 `0`，避免出现空字符串状态。
- Tab 顺序保持为“数值 -> 单位 -> 下一项”，不打断键盘录入。

### 5.5 Measurement Panel

Measurement results reuse the same numeric control cells as element info. Pair results can use a soft blue-violet section background, but not a large glowing card.

### 5.6 Similar Elements Panel

Collapsible inspector section. Count/status uses `StatusBadge`; target selectors render as compact dark rows.

### 5.7 Comment Editor

Textarea, metadata selects and action buttons use matte rounded controls. Page comments and element comments remain visually separate but not card-heavy.

### 5.8 Record List

Compact inspector row blocks:

```text
修改记录       3 / 8
[ 类型 ] [ 状态 ] [ 范围 ]
[ record row ]
[ record row ]
```

Row hover:

- Slight background lift.
- No scale.
- No glow.

### 5.9 Overlay Element Label

- Reuses `overlay-root.ts` Shadow DOM layer.
- Dark matte label.
- Red-pink border/text to match high-contrast selection feedback.
- Small mono font (11-12px).
- Avoid injecting extra host-page styles.

## 6. Scope

### 6.1 Files to Create

1. `extension/src/panel/components/FloatingToolbar.tsx`
2. `extension/src/panel/components/InspectorSection.tsx`
3. `extension/src/panel/components/StatusBadge.tsx`

### 6.2 Files to Modify

1. `extension/src/panel/panel-root.tsx`
   - Sketch-style matte inspector CSS variables and controls.
2. `extension/src/panel/App.tsx`
   - Top toolbar + scrollable inspector panel.
3. `extension/src/panel/components/*`
   - Wrap major areas in `InspectorSection`.
   - Use matte fields, compact rows, status badges and native controls.
4. `extension/src/overlay/overlay-root.ts`
   - Keep red-pink high-contrast selection, measurement and similar-element feedback.
5. `extension/src/content/index.ts`
   - Wire element label updates on selection change.

## 7. Non-goals

V0.5.5 不做：

1. 改变功能逻辑（所有 V0.5 功能保持不变）。
2. 新增数据能力或业务闭环（本轮只补充拖动面板等轻量 UI 交互）。
3. 响应式适配移动端（Panel 仍为桌面浏览器设计）。
4. 主题切换（仅深色主题）。
5. 自定义颜色（固定蓝紫强调，不做主题切换）。
6. 动画库（仅 CSS transitions）。
7. 国际化（仍为中文）。

## 8. Requirements

### R1：深色 Inspector

Given Panel 打开
When 用户查看
Then 背景为哑光深色，控件为深灰胶囊形态，文字高对比

### R2：Sketch-style 控件质感

Given 任意输入、选择、按钮或数值格
When 渲染
Then 使用 40px 左右高度、12px 圆角、弱边界和 subtle hover/focus

### R3：浮动工具栏

Given Panel 打开
When 用户滚动内容
Then 工具栏固定在顶部不滚动

### R4：强调色

Given 按钮 / 徽标 / Panel 内状态
When 渲染
Then 使用蓝紫强调；Overlay 选中与测距反馈使用红粉高对比色

### R5：记录行块

Given 记录列表
When 渲染
Then 每条记录为紧凑暗色行块，hover 只做轻微背景抬升

### R6：元素标签

Given 用户选中元素
When Overlay 渲染
Then 元素上方显示 “Tag 宽 x 高” 标签

### R7：功能完整性

Given 所有 V0.5 功能
When UI 升级后
Then 评论、样式、测距、共享分组全部正常工作

### R8：性能

Given Panel 包含 50 条记录
When 渲染
Then 无明显卡顿（< 100ms）

### R9：面板拖动

Given Panel 打开
When 用户按住标题栏拖动
Then Panel 跟随指针移动，释放后停留在当前位置，且不会被拖出视口

## 9. Implementation Plan

1. Create / update design tokens for matte inspector colors.
2. Replace heavy card wrappers with `InspectorSection`.
3. Update `panel-root.tsx` CSS variables and component styles.
4. Keep Overlay red-pink high-contrast feedback.
5. Bind Panel title-bar dragging in `panel-root.tsx`.
6. Run `cd extension && npm run verify`.
7. Manually reload `extension/dist` in Chrome and check visual fidelity.

## 10. Risks and Edge Cases

| 风险 | 影响 | 降级方案 |
|---|---|---|
| 深色主题对比度不足 | 可访问性问题 | 确保文字对比度 >= 4.5:1 (WCAG AA) |
| 控件过于紧凑 | 信息难读 | 保持 40px 主控件高度和 22px section gap |
| Panel 宽度增加导致遮挡 | 小屏幕体验差 | 保持 360px 上限，内容自适应滚动 |
| Panel 拖到目标元素上方 | 标注时遮挡页面 | 标题栏可拖动，用户可临时移开面板 |
| Panel 被拖出视口 | 无法继续操作 | 拖动和窗口 resize 时保持 8px 安全边距 |
| Overlay 颜色过淡 | 网页上不可见 | Overlay 继续使用红粉 / 红色高对比反馈 |

## 11. Rollout Notes

V0.5.5 开发顺序：

1. Design tokens + base styles。
2. Reusable components（InspectorSection, StatusBadge, FloatingToolbar）。
3. 逐个组件升级（ElementInfo -> StyleEditor -> Measurement -> CommentEditor -> RecordList）。
4. Overlay 色彩统一。
5. App 布局整合。
6. 手动测试 + 打磨。
7. 文档同步。

V0.5 与 V0.5.5 代码均已完成；Chrome 手动验收应联合复核共享范围与刷新后的界面。

## 12. Open Questions

V0.5.5 当前默认决策：

1. 仅深色主题（不做主题切换）（已确认）。
2. 固定蓝紫强调（不做自定义颜色）（已确认）。
3. Panel 宽度 360px（已确认）。
4. 元素标签默认显示（不做开关）（已确认）。
5. Sketch-style matte inspector（已确认）。
6. 主控件圆角 12px（已确认）。
7. 交互动效 160-180ms（已确认）。
